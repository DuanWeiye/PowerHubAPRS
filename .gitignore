# ── 敏感本地配置（含真实服务器域名/APN，绝不提交）──────────────────────────
firmware/config.h

# ── 参考资料 / 第三方库（仅本地保留，不上传）────────────────────────────────
ref/

# ── 个人会话数据（电量日志，无坐标但属私有）──────────────────────────────────
pwrlog_*.csv
pwrlog_*.png

# ── 编译产物 ────────────────────────────────────────────────────────────────
*.bin
/tmp/

# ── 本地工具配置 ────────────────────────────────────────────────────────────
.claude/

# ── 系统杂项 ────────────────────────────────────────────────────────────────
.DS_Store
*.swp
